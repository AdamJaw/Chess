#include "pawn.hpp"
#include <array>

void Pawn::makeListOfPossibleMoves(std::vector<std::vector<Chessman*>> board){
    for(int i=0;i<2;i++){
        while(board.at(c_positionY).at(c_positionX)){
            
        }
        //Pionki białe i czarne poruszają się inaczej inne figury bez zmian
    }
}

